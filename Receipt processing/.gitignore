# Terraform provider binaries (huge files, auto-downloaded by terraform init)
.terraform/

# Terraform state files (contain sensitive data — never commit)
*.tfstate
*.tfstate.backup

# Zipped Lambda code (auto-generated by terraform apply)
.build/
*.zip

# Your actual values (contains email addresses)
terraform.tfvars
